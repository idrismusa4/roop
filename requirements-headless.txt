-r requirements/base.txt

# Optional GPU acceleration: install onnxruntime-gpu alongside the CPU package if CUDA is available.
# Example: pip install onnxruntime-gpu>=1.18.0,<1.20
# Optional face enhancement: install gfpgan (and its torch stack) with
#   pip install gfpgan>=1.3.8,<1.4
# Optional NSFW filtering: install opennsfw2 (and tensorflow) with
#   pip install opennsfw2>=0.10.2,<0.11
