-r requirements/base.txt

customtkinter>=5.2.2,<5.3
# tkinter runtime is provided by the operating system packages.
tkinterdnd2>=0.3.0,<0.4; sys_platform != 'darwin' and platform_machine != 'arm64'
tkinterdnd2-universal>=1.7.3,<1.8; sys_platform == 'darwin' and platform_machine == 'arm64'
